clone_depth: 5

environment:
  nodejs_version: "10"
  access_token:
    secure: df12pQIpAsNHKWrgthAWe5bulP2xgv2AwGnJFS6Cq4KO8EMuo5i8 / EcY71oNB17w

install:
 # Get the latest stable version of Node.js or io.js
  - ps: Install-Product node $env:nodejs_version
  # install modules
  - npm install
  - node --version
  - npm --version
  - npm install
  - npm install hexo-cli -g

build_script:
  - hexo generate

artifacts:
  - path: public

on_success:
  - git config --global credential.helper store
  - ps: Add-Content "$env:USERPROFILE\.git-credentials" "https://$($env:access_token):x-oauth-basic@github.com`n"
  STATIC_SITE_REPO: https://github.com/nvdacn/nvdacn.github.io.git
  TARGET_BRANCH: master
  GIT_USER_NAME: tuJNq + ysGQqmhBKRBFNjQPxBYyFjSInfXYc6TzR6bKk =
  GIT_USER_EMAIL: 8aEPQ + 3xcetTlQDgvcsEqzYa9W + LJ4f9vFAQHVt1KUkOCExkWCxYqpUzYieK7tLlUwh61IZ5qik / SU5Wi0s61A ==
  - cd %TEMP%\static-site
  - del * /f /q
  - for /d %%p IN (*) do rmdir "%%p" /s /q
  - SETLOCAL EnableDelayedExpansion & robocopy "%APPVEYOR_BUILD_FOLDER%\public" "%TEMP%\static-site" /e & IF !ERRORLEVEL! EQU 1 (exit 0) ELSE (IF !ERRORLEVEL! EQU 3 (exit 0) ELSE (exit 1))
  - git add -A
  - if "%APPVEYOR_REPO_BRANCH%"=="master" if not defined APPVEYOR_PULL_REQUEST_NUMBER (git diff --quiet --exit-code --cached || git commit -m "Update Static Site" && git push origin %TARGET_BRANCH% && appveyor AddMessage "Static Site Updated")

